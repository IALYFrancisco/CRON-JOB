name: Night Light 💡

on:
  schedule:
    - cron: '*/5 * * * *'  # Runs every 5 minutes (UTC)
  workflow_dispatch:        # Allows manual trigger

jobs:
  keep-awake:
    runs-on: ubuntu-latest

    steps:
      - name: Ping my portolio_3 🙍‍♂️
        run: curl -s https://portfolio-4n0j.onrender.com

      - name: Ping ME application 📉
        run: curl -s https://me-ne9o.onrender.com